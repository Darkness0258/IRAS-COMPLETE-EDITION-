# IRAS Cloud Deployment

The recommended v2.1 deployment is now:

```text
Render Free + Supabase Free + OpenRouter
```

See:

`RENDER_SUPABASE_DEPLOY.md`

The older Koyeb deployment path is no longer the recommended configuration for this project.
